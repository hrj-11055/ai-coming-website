# Legacy Sync Scripts

Archived scripts retained for traceability. These are not part of the active JSON-mainline workflow.

- `历史同步设置脚本（已删除）`: references missing `历史同步执行脚本（已删除）`.
- `历史自动导入设置脚本（已删除）`: references missing `历史自动导入执行脚本（已删除）`.

Use active scripts in project root instead:

- `server-sync.sh`
- `setup-server-sync.sh`
- `sync-json-news.sh`
