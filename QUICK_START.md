# 🚀 JSON日报快速入门

## ✅ 已完成的配置

### 1. 服务器目录
```bash
/var/www/json/reports/  # JSON日报存放目录
```

### 2. 自动同步服务
- ✅ 每60秒自动同步
- ✅ 状态：运行中

### 3. API支持
- ✅ 支持两种JSON格式
- ✅ 自动识别并显示

## 📝 支持的JSON格式

### 格式1：直接数组
```json
[
  {
    "title": "标题",
    "summary": "摘要",
    ...
  }
]
```

### 格式2：包含articles字段
```json
{
  "articles": [
    {
      "title": "标题",
      "summary": "摘要",
      ...
    }
  ]
}
```

## 🎯 三种使用方式

### 方式1：上传到服务器（推荐）

```bash
# 1. 准备JSON文件
cat > daily-report.json << 'EOF'
{
  "articles": [
    {
      "title": "今日AI新闻",
      "summary": "详细内容...",
      "source_url": "https://...",
      "source_name": "来源",
      "category": "技术",
      "published_at": "2026-02-09T09:00:00.000Z"
    }
  ]
}
EOF

# 2. 上传到服务器
scp daily-report.json root@8.135.37.159:/var/www/json/reports/news-2026-02-09.json

# 3. 等待60秒自动同步，或手动触发
./sync-data-daemon.sh

# 4. 查看效果
open http://localhost:3000/news.html
```

### 方式2：在服务器上直接生成

```bash
ssh root@8.135.37.159

# 直接在服务器上创建JSON文件
cat > /var/www/json/reports/news-2026-02-09.json << 'EOF'
{
  "articles": [...]
}
EOF

# 退出SSH，文件会自动同步到Mac
exit
```

### 方式3：使用API导入

```bash
curl -X POST http://localhost:3000/api/news/batch \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d @your-report.json
```

## 📊 验证效果

### 1. 检查同步
```bash
# 查看本地文件
ls -lh data/news-2026-02-09.json

# 查看同步日志
tail -f /tmp/aicoming-sync.log
```

### 2. 测试API
```bash
# 获取所有日期
curl -s http://localhost:3000/api/news/dates | python3 -m json.tool

# 获取特定日期
curl -s http://localhost:3000/api/news/date/2026-02-09 | python3 -m json.tool
```

### 3. 查看网页
访问 http://localhost:3000/news.html

应该看到：
- ✅ 左侧时间轴显示2026-02-09
- ✅ 点击可查看文章列表
- ✅ 文章正常显示

## 🎉 测试结果

当前状态：
- ✅ 2026-02-09已显示（2篇示例文章）
- ✅ API正确读取
- ✅ 网页正常显示

## 💡 最佳实践

### 文件命名
推荐使用：`news-YYYY-MM-DD.json`

例如：
- `news-2026-02-09.json`
- `news-2026-02-10.json`

### 必填字段
```json
{
  "title": "标题（必填）",
  "summary": "摘要（必填）",
  "source_url": "链接（必填）",
  "source_name": "来源（必填）",
  "category": "技术（可选，默认技术）",
  "importance_score": 7（可选，默认5分）",
  "published_at": "2026-02-09T09:00:00.000Z（可选）"
}
```

### 自动归档
- 导入时会自动归档旧新闻
- 每天的新闻会保存到独立文件
- 历史文章可随时查看

## 🔄 下次日报流程

1. **生成JSON文件**（使用您的日报生成工具）
2. **上传到服务器**：`scp news.json root@8.135.37.159:/var/www/json/reports/news-$(date +%Y-%m-%d).json`
3. **等待自动同步**（60秒内）
4. **刷新网页查看**

就这么简单！🎉
