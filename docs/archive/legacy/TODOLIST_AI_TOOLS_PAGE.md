# AI工具集子页面 ToDoList（明日执行）

## 目标
- 新增一个独立子页面展示 AI 工具集，参考 `ai-bot.cn` 的信息密度与分组方式。
- 页面核心布局：`每行 6 个工具`，其中 `左 3 个国际工具`、`右 3 个国内工具`。
- 使用你已收集好的 logo icon，保持加载快、可维护、可持续扩充。

## 范围
- 范围内：新页面（`tools.html`）、前端模块、工具数据组织、logo 接入、导航入口。
- 范围外：后台管理 UI 重构、数据库迁移、现有 news 页面交互改版。

## P0 需求冻结（0.5h）
- [ ] 明确一级分类清单（例如：对话模型、代码助手、设计工具、视频工具、办公效率、Agent平台）。
- [ ] 每个分类确认展示行数上限（建议 2-4 行）。
- [ ] 明确工具卡最小字段：`name / logo / website / region / category / tags`。
- [ ] 固定排版规则：桌面端每行 6 个（左3国际+右3国内），移动端自动降级为 2 列。

验收：
- [ ] 形成可执行字段表和分类清单，不再改口径。

## P1 数据与资源准备（1-1.5h）
- [ ] 规范 logo 命名：`logos/tools/<slug>.png`（全小写、短横线）。
- [ ] 建立工具数据文件（优先复用现有 `data/tools.json`）。
- [ ] 补齐 `region`（国内/国际）与 `categories` 字段，确保可分组。
- [ ] 写数据校验脚本（可选）：检测重复 slug、缺失 logo、缺失 website。

验收：
- [ ] 抽样 20 个工具，logo 全可访问、字段完整。

## P2 页面骨架与模块（2h）
- [ ] 新增页面：`/Users/MarkHuang/ai-coming-website/tools.html`。
- [ ] 新增入口脚本：`/Users/MarkHuang/ai-coming-website/frontend/tools-page.js`。
- [ ] 新增模块（可先 3 个）：
- `frontend/modules/tools-data.js`（拉取/缓存数据）
- `frontend/modules/tools-layout.js`（按分类 + 国内/国际切行）
- `frontend/modules/tools-render.js`（渲染网格和卡片）
- [ ] 在顶部导航加入“AI工具集”入口（`index.html`、`news.html` 同步）。

验收：
- [ ] 打开 `tools.html` 可看到分组框架和占位卡。

## P3 排版实现（核心）（2-3h）
- [ ] 分类区块按“每行6个”渲染。
- [ ] 行内强约束：左侧容器放 3 个国际，右侧容器放 3 个国内。
- [ ] 某侧不足 3 个时，用空占位保持网格对齐（避免整行错位）。
- [ ] 卡片信息：logo、名称、标签（可选）、外链按钮。
- [ ] hover 态：轻微阴影/位移，点击新开页。

验收：
- [ ] 任意分类都满足“左3国际+右3国内”的行结构。
- [ ] 桌面和移动都不破版。

## P4 交互与性能（1.5h）
- [ ] 首屏先渲染前 2 个分类，其余分组延后渲染（requestIdleCallback / setTimeout）。
- [ ] logo 懒加载（`loading="lazy"`），避免一次性解码过多图。
- [ ] 数据请求加 30-60s 内存缓存（前端）与失败兜底提示。
- [ ] 搜索和筛选（按分类/地区）支持前端即时过滤。

验收：
- [ ] 首屏 1s 内看到内容骨架，2s 内可交互。
- [ ] 慢网下不白屏，有骨架和错误提示。

## P5 联调与回归（1h）
- [ ] 回归导航：`index -> tools -> news` 路由可往返。
- [ ] 回归外链：随机 20 个工具跳转正常。
- [ ] 回归样式：Chrome/Safari + 手机宽度下检查。
- [ ] 记录已知问题和下一轮优化项。

验收：
- [ ] 手工回归清单全部通过。

## 明日交付定义
- [ ] `tools.html` 可上线使用（非 Demo）。
- [ ] 工具数据可继续批量维护，不需要改渲染代码。
- [ ] 满足你指定的排版规则：每行 6 个，左国际 3，右国内 3。

## 夜间自动运行（可选）
- [ ] 自动任务A：每小时扫描 `logos/tools` 并校验缺失映射。
- [ ] 自动任务B：每小时生成“新增工具候选清单”（不直接发布）。
- [ ] 自动任务C：输出次日待办报告（哪些分类缺工具、哪些logo缺失）。

说明：自动任务建议先跑“只出报告不改数据”，避免睡觉期间误改线上内容。
